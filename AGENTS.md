# AGENTS.md

适用于本仓库的通用协作与实现原则。项目内更具体的规则、架构文档和任务要求优先。

## 通用协作与实现原则

### 1. 先证据，后决策

- 先检查相关代码、文档、类型、测试、依赖与仓库约定，再决定实现方式。
- 仅当以下信息无法可靠推断、且会实质改变方案时向用户提问：
  - 用户意图或验收标准；
  - 公开 API、外部契约或持久化数据；
  - 安全、隐私、权限、支付或不可逆操作；
  - 存在多个影响明显不同的可行方案。

### 2. 最小且可持续的实现

- 选择能够完整满足当前需求的最简单实现。
- 不为“未来可能需要”预先增加抽象、配置、间接层或兼容路径。
- 优先选择最小的长期可维护方案；不要交付明知很快需要推翻的临时代码。
- 只有在临时方案的范围、责任人、移除条件和后续路径明确时，才可采用临时方案。
- 代码行数不是质量指标；以职责清晰、复杂度合理和满足当前需求为准。

### 3. 清晰职责与模块边界

- 保持组件和模块职责清晰、内聚，并遵循仓库已有边界。
- 不要仅为“模块化”而拆分文件、组件或层级。
- 仅当新层或新抽象承载明确且持久的职责时才引入它。
- 默认遵循既有代码风格；若其与安全、正确性、明确需求或仓库标准冲突，以后者为准。

### 4. 优先复用成熟能力

- 编写自定义实现或添加新依赖前，先检查项目已有依赖、工具、类型定义和文档。
- 不要未经验证就假定已有库不具备所需能力。
- 成熟、维护良好的库若能降低总体复杂度或提升可靠性，应优先采用。
- 引入新依赖时，确认其确实减少复杂度，并符合项目的体积、维护、安全和许可约束。

### 5. 向后兼容与遗留路径

- 默认不为历史行为新增兼容层、降级逻辑或迁移垫片。
- 发现过时的兼容路径、fallback、旧 API 或迁移逻辑时，先追踪其当前消费者、外部契约和数据影响。
- 删除此类路径属于高风险决策：应报告证据、受影响消费者、迁移/数据影响和回滚方式，并向用户请求明确授权。
- 若仍存在真实消费者或有效外部契约，只保留满足该契约所必需的兼容性；不要无依据扩展兼容范围。
- 所有修复和改动默认只做根因修复，用正确的逻辑覆盖错误而不是打补丁。

### 6. 控制变更范围

- 每一处改动都应直接服务于用户请求，或是必要的集成、测试、生成物和验证工作。
- 不进行无关重构、清理、格式化或“顺手改进”。
- 如果发现无关的死代码、重复逻辑或架构问题，低风险直接处理，高风险应报告它们，不要自行处理。
- 删除既有代码、依赖、测试或文档，必须有明确授权和消费者证据。
- 清理由当前改动造成的无用 import、变量、注册项或测试残留。

### 7. 风险相称的健壮性与验证

- 对用户输入、外部服务、持久化、并发和安全边界处理真实存在的失败风险。
- 不要为纯粹臆测的场景堆砌防御代码，优先快速失败，而不是掩盖问题。
- 为变更选择最小且可信的验证方式：聚焦测试、构建、静态检查或可复现的手工验证。
- 修复缺陷时，如能稳定复现且成本合理，优先添加回归测试。
- 现有测试、快照和断言在证据证明需求已改变前应视为有效契约。
- 完成时报告已执行的验证，以及未验证内容和原因。

### 8. 计划与沟通

- 仓库已有计划、架构或工作流约定时，优先遵循仓库约定。
- 始终使用中文与用户沟通。

## 项目特定约束
在跑需要联网的命令时可以连接代理
```
export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=http://127.0.0.1:7890
```
## Git 提交

- 每次使用 `git commit` 时，提交信息的具体描述必须使用中文；允许保留 Conventional Commits 的英文类型前缀，例如 `feat:实现了某个功能`。

## 项目定位与测试策略

- 默认只实现冒烟级测试，聚焦快乐路径；测试应足够务实、足够支撑当前功能。
- 不强制要求测试覆盖率，也不为追求覆盖率补充穷举式用例。
- 只有代码逻辑确有必要时，才补充完整的异常与边界测试。

## 测试执行策略

- 开发过程中必须选择能够直接证明当前变更的最窄测试，不得默认运行完整数据库回归。
- 普通需求完成时默认依次执行：
  1. 与当前变更直接相关的目标测试；
  2. `pnpm run verify`；
  3. 涉及 Schema、Repository、事务或锁语义时执行对应的 `db:test:milestone`；涉及应用服务、HTTP/SSE、Session 命令或 Agent 协调贯穿 PostgreSQL 时执行对应的 `postgres:e2e:milestone`。同时影响两层时必须串行执行两者。
- 对应的 `db:test:full` 或 `postgres:e2e:full` 只在以下情况执行：
  - 用户明确要求；
  - 合并主分支或发布前；
  - 修改共享事务、锁、Schema、migration 或数据库测试基础设施；
  - 多个里程碑之间存在无法由定向测试排除的影响。
- 同一个任务中每套 full 最多主动执行一次；发布前需要两套 full 证据时必须串行执行。
- 任一 full 失败后不得直接反复重跑；必须先用同一套命令定向诊断失败里程碑。修复仅涉及该里程碑测试或预算时，优先重跑该里程碑；只有变更可能影响同套其他阶段时才重新执行 full。
- 所有远程 PostgreSQL 测试必须串行执行，不得并行运行 database 与 E2E，也不得并行运行 full 和 milestone。
- 最终报告必须明确列出两套远程测试各自已执行和未执行的范围，不得把 milestone 通过描述为对应 full 通过，也不得把 `db:test:full` 通过描述为 PostgreSQL E2E 通过。

## 代码简化

- `simplify-codebase` 触发后必须先阅读 `docs/DEFENSIVE_PATTERNS.md`；不得把版本 Codec、legacy reader、migration、事务防御或 SSE 生命周期机械合并。
- 孤立的 unused import、variable 或类型属于普通 lint，不调用 `simplify-codebase`。轻量清理依次执行定向测试、`pnpm run simplify:light`；定向测试由改动所在 workspace 选择。
- 深度清理依次执行 lint/unused、Knip、重复报告、`pnpm run verify`、`pnpm run build` 和 `pnpm --filter @tx-holdem-coach/server run verify:migration-assets`，可用 `pnpm run simplify:deep` 聚合后续步骤。
- 数据库相关变更仍严格遵循本文件既有 milestone/full 规则；不得把远程数据库测试硬编码进每次 deep cleanup。
- 不得为了通过分析器扩大 ignore。每个排除项必须有仓库事实理由；工具结果只产生候选，删除前仍需消费者与契约证据。
